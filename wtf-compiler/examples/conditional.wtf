package wtf:example@0.0.1

func double_cap(x: s64, cap: s64) -> s64 {
    let y = x * 2
    if y > cap {
       return cap
    } else {
       return y
    }
}

record point {
    x: s64 
    y: s64
}

func double_point(p: point) -> point {
    return p
}
