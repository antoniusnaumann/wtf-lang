package wtf:example@0.1.0

export func double_cap(x: s64, cap: s64) -> s64 {
    let y = x * 2
    if y > cap {
       return cap
    } else {
       return y
    }
}

export func main() {
   // return double_cap(5, 11)
   // return 12 * 2
}
